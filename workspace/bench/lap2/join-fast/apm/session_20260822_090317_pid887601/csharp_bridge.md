# Native → C# → Mod API bridge

## Recommended order of work

### 1. Lock / futex contention (priority=14.01, conf=0.8)
Native evidence (2.5% of sampled weight): __pthread_cond_wait_common, ___pthread_cond_timedwait64, ___pthread_cond_wait, ___pthread_mutex_unlock
Structured layer signals: slow_futex_lines=16.0
C# surface:
  - Managed lock (lock/Monitor) or native mutex under Mono/Unity
  - Often GC STW, thread pool, or net package queues
Harmony targets (start here):
  - `GameManager.gmUpdate`
  - `ConnectionManager.Update`
  - `ThreadManager.UpdateMainThreadTasks`
Mod / config levers:
  - Host APM: open sync/futex.bt.out waiter stacks
  - If stacks show mono_gc_* → fix allocations (see gc_mono)
  - Avoid blocking IO inside Harmony on main thread
  - Do not hold locks across SpawnEntityInWorld storms generated by a controlled scenario
Experiment: Apply one lever below, re-run the identical `7dtd-server-apm scenario run` workload, then `7dtd-server-apm compare` the before/after sessions.

### 2. Mono / Boehm GC (priority=14.0, conf=0.8)
Native evidence (2.5% of sampled weight): [libmonobdwgc-2.0.so], mono_gc_collect_a_little
Structured layer signals: collect_a_little_hits=50050.0
C# surface:
  - System.GC pressure from managed allocations on the main sim thread
  - Common alloc sites: LINQ in hot loops, string concat in AI, new List per tick
Harmony targets (start here):
  - `GameManager.gmUpdate`
  - `World.TickEntities`
  - `EntityAlive.updateTasks`
  - `EntityAlive.OnUpdateLive`
  - `EAIManager.Update`
Mod / config levers:
  - EfficientServer: APM bridge DeepMode → find which section correlates with GC
  - Harmony: remove allocs in Prefix/Postfix on listed targets
  - Pool List/Dictionary; avoid string.Format in entity tick
  - XML: reduce entity count (MaxSpawnedZombies) to cut GC volume
Experiment: Apply one lever below, re-run the identical `7dtd-server-apm scenario run` workload, then `7dtd-server-apm compare` the before/after sessions.

### 3. Blocking disk I/O (priority=4.5, conf=0.3)
Structured layer signals: slow_block_lines=1.0
C# surface:
  - Synchronous file IO on sim thread (region/chunk save/load)
  - World save, prefab, player data, dynamic mesh files
Harmony targets (start here):
  - `GameManager.gmUpdate`
  - `World.Save*`
  - `Chunk* save/load paths (version-specific names)`
Mod / config levers:
  - Move Saves to local NVMe
  - Increase save interval / avoid save spam commands
  - EnableMapRendering=false
  - Inspect APM io/vfs.bt.out openat paths + slow stacks
Experiment: Apply one lever below, re-run the identical `7dtd-server-apm scenario run` workload, then `7dtd-server-apm compare` the before/after sessions.

## Top managed bridge sections



## Top native frames (folded/speedscope)

- `[jit]` ×31131
- `[UnityPlayer.so]` ×5314
- `[perf-887601.map]` ×1231
- `[libc.so.6]` ×1192
- `[libmonobdwgc-2.0.so]` ×1062
- `__pthread_cond_wait_common` ×389
- `___pthread_cond_timedwait64` ×288
- `[steamclient.so]` ×234
- `internal_syscall_cancel` ×135
- `__pthread_cleanup_pop` ×119
- `___pthread_cond_wait` ×104
- `___pthread_mutex_unlock` ×100
- `__pthread_mutex_unlock_usercnt` ×94
- `__condvar_confirm_wakeup` ×92
- `lll_mutex_unlock_optimized` ×88
- `syscall_cancel` ×80
- `mono_threads_exit_gc_safe_region_internal` ×68
- `GC_dirty_inner` ×66
- `__clock_nanosleep` ×56
- `usleep` ×50
