# Native → C# → Mod API bridge

## Recommended order of work

### 1. Lock / futex contention (priority=13.69, conf=0.79)
Native evidence (2.2% of sampled weight): __pthread_cond_wait_common, ___pthread_cond_timedwait64, ___pthread_mutex_unlock, __pthread_mutex_unlock_usercnt
Structured layer signals: slow_futex_lines=4.0
C# surface:
  - Managed lock (lock/Monitor) or native mutex under Mono/Unity
  - Often GC STW, thread pool, or net package queues
Harmony targets (start here):
  - `GameManager.gmUpdate`
  - `ConnectionManager.Update`
  - `ThreadManager.UpdateMainThreadTasks`
Mod / config levers:
  - Host APM: open sync/futex.bt.out waiter stacks
  - If stacks show mono_gc_* → fix allocations (see gc_mono)
  - Avoid blocking IO inside Harmony on main thread
  - Do not hold locks across SpawnEntityInWorld storms generated by a controlled scenario
Experiment: Apply one lever below, re-run the identical `7dtd-apm scenario run` workload, then `7dtd-apm compare` the before/after sessions.

### 2. Mono / Boehm GC (priority=13.41, conf=0.79)
Native evidence (2.0% of sampled weight): [libmonobdwgc-2.0.so]
Structured layer signals: collect_a_little_hits=33577.0
C# surface:
  - System.GC pressure from managed allocations on the main sim thread
  - Common alloc sites: LINQ in hot loops, string concat in AI, new List per tick
Harmony targets (start here):
  - `GameManager.gmUpdate`
  - `World.TickEntities`
  - `EntityAlive.updateTasks`
  - `EntityAlive.OnUpdateLive`
  - `EAIManager.Update`
Mod / config levers:
  - EfficientServer: APM bridge DeepMode → find which section correlates with GC
  - Harmony: remove allocs in Prefix/Postfix on listed targets
  - Pool List/Dictionary; avoid string.Format in entity tick
  - XML: reduce entity count (MaxSpawnedZombies) to cut GC volume
Experiment: Apply one lever below, re-run the identical `7dtd-apm scenario run` workload, then `7dtd-apm compare` the before/after sessions.

## Top managed bridge sections



## Top native frames (folded/speedscope)

- `[jit]` ×92212
- `[perf-887601.map]` ×5515
- `[UnityPlayer.so]` ×5051
- `[libc.so.6]` ×3753
- `[libmonobdwgc-2.0.so]` ×2270
- `__pthread_cond_wait_common` ×947
- `___pthread_cond_timedwait64` ×816
- `__pthread_cleanup_pop` ×402
- `mono_thread_info_uninstall_interrupt` ×333
- `__condvar_confirm_wakeup` ×313
- `[steamclient.so]` ×248
- `GC_suspend_handler` ×223
- `___pthread_mutex_unlock` ×200
- `__pthread_mutex_unlock_usercnt` ×193
- `lll_mutex_unlock_optimized` ×184
- `GC_dirty_inner` ×159
- `___pthread_mutex_lock` ×144
- `___pthread_cond_wait` ×137
- `mono_threads_exit_gc_safe_region_internal` ×130
- `internal_syscall_cancel` ×130
