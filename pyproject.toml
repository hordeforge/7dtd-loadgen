[project]
name = "7dtd-loadgen"
version = "0.1.0"
description = "Controlled LiteNetLib load generation for 7 Days to Die dedicated servers"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
